using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;
using UnityEngine.UI;

public class LobbyManager : MonoBehaviour
{
    public Button StartButton, LobbyButton, InformationButton;
    public GameObject StartPanel, LobbyPanel, InformationPanel;
    public Button MatchButton, CreateCustomButton, JoinCustomButton;
    public GameObject MatchModePanel, CreateCustomPanel, JoinCustomPanel;
    public Button OkayButton, BackLobbyButton;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public 
}
